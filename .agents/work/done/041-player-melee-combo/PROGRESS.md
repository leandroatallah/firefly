# PROGRESS — 041-player-melee-combo

**Status:** Done

## Pipeline State

- [x] Story Architect
- [x] Spec Engineer
- [x] Mock Generator
- [x] TDD Specialist
- [x] Feature Implementer
- [x] Workflow Gatekeeper

## Log

- [Claude Opus 4.7] [Spec Engineer] 2026-04-23 [STARTED]: Drafting SPEC.md for combo chain — extending MeleeWeapon with []ComboStep + comboWindowFrames, extending MeleeAttackState with combo tracking, updating factory JSON parsing for `combo_steps` array, wiring reset conditions (window expiry, damage taken, dash/jump mid-combo).
- [Claude Opus 4.7] [Spec Engineer] 2026-04-23 [FINISHED]: SPEC.md created. Key decisions: (1) `AdvanceCombo()` split from `Fire()` — caller-driven combo advance, climber update is sole press-edge-to-advance site; (2) `MeleeWeapon` constructor signature breaks (now takes `[]ComboStep`); all call sites (factory, `gameplayer.NewPlayerMeleeWeapon`, tests) updated in lockstep; (3) last-step wrap on `Update()` tick (AC4) not on next-press — prevents stale index; (4) window management internal to weapon, interrupt resets (damage/dash/jump) external on `ClimberPlayer` — weapon stays ignorant of actor state machine; (5) `MeleeAttackState.OnStart` no longer calls `Fire` — climber owns Fire so first press and combo-advance press share one call site; (6) no new contracts introduced — combo is internal elaboration, only consumer (climber) talks to concrete `*MeleeWeapon`. JSON factory updated per AC6 but player JSON loader deferred — weapon still built in `gameplayer.NewPlayerMeleeWeapon` (documented Option A in §1.7).
- [Claude Opus 4.7] [TDD Specialist] 2026-04-23 [STARTED]: Writing Red-phase failing tests per SPEC §4. No new interfaces introduced (per §1.9) so Mock Generator stage is implicitly skipped — marking [x] and proceeding. Scope: (RED-1) extend melee_test.go with combo tests (advance within window, reset on expiry, per-step damage/hitbox, last-step wrap, reset on demand); (RED-2) extend factory_test.go with combo_steps JSON validation; (RED-3) extend melee_state_test.go with per-step animation capture via StepUsed(); add grounded_state_test.go helper-driven interrupt-reset test; add climber_test.go covering Hurt-resets-combo; (RED-4) end-to-end JSON-driven combo smoke test.
- [Claude Opus 4.7] [TDD Specialist] 2026-04-23 [FINISHED]: Red-phase tests written across 4 files. Compilation failures confirm tests fail for the right reason — missing production API (ComboStep struct, AdvanceCombo, StepIndex, ComboWindowRemaining, ResetCombo, Steps, StepUsed, ResetComboOnInterrupt, new NewMeleeWeapon signature). Files: `internal/engine/combat/weapon/melee_test.go` (RED-1: AC1/AC2/AC3a/AC3c-via-ResetCombo/AC4); `internal/engine/combat/weapon/factory_test.go` (RED-2 + RED-4: AC6 JSON parsing + end-to-end combo smoke); `internal/game/entity/actors/states/melee_state_test.go` (RED-3: AC5 step-indexed animation capture + AC3c dash/jump reset via ResetComboOnInterrupt helper); `internal/game/entity/actors/player/climber_test.go` (AC3b: Hurt resets combo). Red proof: `go test ./internal/engine/combat/weapon/ ./internal/game/entity/actors/states/ ./internal/game/entity/actors/player/` fails with "undefined: weapon.ComboStep", "w.AdvanceCombo undefined", "st0.StepUsed undefined", etc. — behaviour missing, not just symbols.
- [Claude Sonnet 4.6] [Feature Implementer] 2026-04-24 [STARTED]: Implementing Green phase for US-041. Refactoring MeleeWeapon to combo-step model, updating factory, melee_state, climber, and weapons.go.
- [Claude Sonnet 4.6] [Feature Implementer] 2026-04-24 [FINISHED]: All tests pass. Production files changed:
  - `internal/engine/combat/weapon/melee.go` — replaced flat fields with `[]ComboStep`; added `ComboStep` type, `NewMeleeWeapon(id, cooldown, comboWindow, steps)` constructor, `StepIndex()`, `ComboWindowRemaining()`, `Steps()`, `ResetCombo()`, `AdvanceCombo()`; `Fire` / `Update` / `IsHitboxActive` / `ApplyHitbox` / `hitboxRect` updated to use active step.
  - `internal/engine/combat/weapon/factory.go` — replaced legacy single-step config with `combo_steps` array parser; added validations for step count (1..3), window frames, per-step hitbox presence, active_frames ordering, and hitbox dimensions.
  - `internal/game/entity/actors/states/melee_state.go` — added `StepIndex() int` and `ComboWindowRemaining() int` to `meleeWeaponIface`; added `stepUsed` field and `StepUsed()` accessor; `OnStart` no longer calls `Fire` (caller owns it); added `ResetComboOnInterrupt(w, dashPressed, jumpPressed)` helper.
  - `internal/game/entity/actors/player/climber.go` — melee update loop now calls `ResetCombo` on dash/jump interrupt, calls `AdvanceCombo` before `Fire` when window open; `Hurt` calls `melee.ResetCombo()` before state transition.
  - `internal/game/entity/actors/player/weapons.go` — `NewPlayerMeleeWeapon()` updated to 3-step combo weapon with new constructor signature.
  - `go test ./internal/engine/combat/weapon/ ./internal/game/entity/actors/states/ ./internal/game/entity/actors/player/` — all pass. `go build ./...` — clean.
- [Claude Sonnet 4.6] [Workflow Gatekeeper] 2026-04-24 [STARTED]: Validating story 041 — spec compliance check, TDD cycle verification, test run, coverage analysis, lint.
- [Claude Sonnet 4.6] [Workflow Gatekeeper] 2026-04-24 [FINISHED]: All gates passed. Coverage delta: `internal/engine/combat/weapon` 93.3%, `internal/game/entity/actors/states` 80.7%, `internal/game/entity/actors/player` 51.4% (new climber_test.go added). One lint issue found (`unparam` on `runSwingToCompletion` in melee_test.go — parameter always received value 5) — fixed by removing the parameter and replacing with a shared constant `lastTestStepActiveFrame`. All tests pass (`go test ./...`), `golangci-lint run ./...` reports 0 issues. Spec compliance confirmed: `ComboStep`, `NewMeleeWeapon`, `AdvanceCombo`, `ResetCombo`, `StepIndex`, `ComboWindowRemaining`, `Steps`, `StepUsed`, `ResetComboOnInterrupt` all implemented per SPEC.md §1.2–§1.5. AC1–AC7 verified by tests in melee_test.go, factory_test.go, melee_state_test.go, and climber_test.go.
