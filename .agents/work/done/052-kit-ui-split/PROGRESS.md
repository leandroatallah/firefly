# PROGRESS — 052-kit-ui-split

**Status:** Done

## Pipeline State
- [x] Story Architect
- [x] Spec Engineer
- [x] Mock Generator
- [x] TDD Specialist (rework completed 2026-05-06 — coverage 89.8%)
- [x] Feature Implementer
- [x] Workflow Gatekeeper

## Log
- Story Architect 2026-05-04: USER_STORY.md created.
- Story Architect 2026-05-04: USER_STORY.md revised — scope corrected: menu stays in engine/ui/menu/ (primitive mechanic, engine-internal imports); only speech.Manager (dialogue.go) moves to kit/ui/speech/. Title, Background, AC, Out of Scope, and Domain Notes updated accordingly.
- [Opus 4.7] [Spec Engineer] 2026-05-05 [STARTED]: Drafting SPEC.md for 052-kit-ui-split; folder moved backlog → active via scripts/story.sh start.
- [Opus 4.7] [Spec Engineer] 2026-05-05 [FINISHED]: SPEC.md created. Key decisions: (1) A naive file move is insufficient — `engine/app/context.go` and `engine/sequences/commands.go` currently reference the concrete `*speech.Manager`, so a new contract package `internal/engine/contracts/dialogue/` is introduced exposing `dialogue.Manager` interface plus `BubbleSpeechID`/`StorySpeechID` constants; engine code depends on the interface, kit's `Manager` implements it, game wires the concrete. (2) Kit re-exports the ID constants as aliases so the story AC wording ("constants travel with Manager") still holds for game-layer callers. (3) Red Phase comprises a layer-rule `go list -deps` test, a `var _ dialogue.Manager = (*Manager)(nil)` compile-assertion test in the new kit package, and the relocated behavioural tests for `Manager`. (4) `mocks_test.go` move depends on whether primitive speech tests reference `mockSpeech` — implementer must verify. New contract introduced → next agent is Mock Generator.
- [Opus 4.7] [TDD Specialist] 2026-05-05 [FINISHED]: Red phase authored. Files: `internal/engine/dependency_test.go` (layer-rule safety net — `engine` must not depend on `kit`/`game`); `internal/kit/ui/speech/dialogue_test.go` (compile-time `var _ dialogue.Manager = (*Manager)(nil)` assertion plus behavioural regressions for `NewManager` / `SetActiveSpeech` / `SetSpeech` no-op / `ShowMessages` initialising state and triggering `Show()`); `internal/kit/ui/speech/mocks_test.go` (white-box `mockSpeech` helper satisfying `engine/ui/speech.Speech`). Red proof: `go test ./internal/kit/ui/speech/...` fails to compile because `internal/engine/contracts/dialogue` and `internal/kit/ui/speech.Manager` do not exist; `go test ./internal/engine/ -run TestEngineDoesNotDependOnKitOrGame` fails because `go list -deps ./internal/engine/...` errors on the same missing contract package — both failures will resolve once the Feature Implementer creates the contract, moves `Manager` into `kit/ui/speech`, and rewires engine consumers through the interface.
- [Sonnet 4.6] [Feature Implementer] 2026-05-05 [FINISHED]: All production files created/updated. Green phase confirmed — all tests in `internal/engine/...` and `internal/kit/ui/speech/` pass. Layer-rule checks pass (engine has no kit/game deps; kit has no game deps). Files: `internal/engine/contracts/dialogue/dialogue.go` (NEW), `internal/kit/ui/speech/doc.go` (NEW), `internal/kit/ui/speech/dialogue.go` (NEW — Manager moved from engine), `internal/engine/app/context.go` (DialogueManager field changed to interface), `internal/engine/app/app_test.go` (uses MockDialogueManager), `internal/engine/sequences/commands.go` (dialogueManager field changed to interface, BubbleSpeechID from dialogue contract), `internal/engine/sequences/commands_test.go` (uses mocks.MockDialogueManager), `internal/engine/mocks/audio_speech_vfx.go` (old incomplete MockDialogueManager removed), `internal/engine/dependency_test.go` (fixed to run from module root with precise regex), `internal/game/app/setup.go` (uses kitspeech.NewManager), `internal/game/ui/speech/bubble.go` (uses kitspeech.BubbleSpeechID), `internal/game/ui/speech/story.go` (uses kitspeech.StorySpeechID). Deleted: `internal/engine/ui/speech/dialogue.go`, `internal/engine/ui/speech/dialogue_test.go`, `internal/engine/ui/speech/mocks_test.go`.
- [Sonnet 4.6] [Workflow Gatekeeper] 2026-05-05 [REJECTED]: Coverage gate failed. `internal/kit/ui/speech/` measured at 19.7% — required minimum is 80% (SPEC §8 post-condition 8, story AC "Coverage non-negative: kit/ui/speech/ reaches 80%+ coverage"). Root cause: the TDD Specialist's `dialogue_test.go` only carried `TestManager`, `TestManager_ShowMessages`, and `TestManager_ImplementsDialogueContract` into the kit package. The original `engine/ui/speech/dialogue_test.go` contained five additional tests that were deleted with the file and not ported: `TestManager_Update`, `TestManager_Draw`, `TestManager_ApplyDefaultSpeechAudio_Rotates`, `TestManager_StartSpeechAudio_UsesLineAudio`, `TestManager_TypingSound_Rotates`. The `typingSpeech` mock type and the `config.Set` setup that those tests depend on also need to move. All other quality gates PASS (layer-rule deps clean, golangci-lint 0 issues, all existing tests green, contract/alias/doc.go correct, constitution Kit row present, no game strings in kit). Backtrack to: **TDD Specialist** — add the missing tests to `internal/kit/ui/speech/dialogue_test.go` so that coverage reaches >= 80% before re-engaging Feature Implementer (if any new production helpers are needed) and Workflow Gatekeeper.
- [Opus 4.7] [TDD Specialist] 2026-05-06 [FINISHED — REWORK]: Ported the 5 missing tests from `git show HEAD~1:internal/engine/ui/speech/dialogue_test.go` into `internal/kit/ui/speech/dialogue_test.go` (`TestManager_Update`, `TestManager_Draw`, `TestManager_ApplyDefaultSpeechAudio_Rotates`, `TestManager_StartSpeechAudio_UsesLineAudio`, `TestManager_TypingSound_Rotates`) along with the `typingSpeech` helper and `config.Set` setup. Adjusted imports for the new package path (`engine/audio`, `engine/data/config`, `engine/input`, `ebiten`). Added focused behavioural tests to lift coverage above the 80% gate: `TestManager_SetTypingSound`, `TestManager_ClearSpeechAudioQueue`, `TestManager_ApplyDefaultSpeechAudio_NoDefault_ClearsQueue`, `TestManager_SetSpeechSkipEnabled`, `TestManager_StopSpeechAudio_NoManager_ClearsQueue`, `TestManager_GetActiveSpeech_ReturnsActive`, `TestManager_Update_ConfirmAdvancesLine` (drives Confirm-press path via `input.CommandsReader` stub), `TestManager_Update_ConfirmHidesAtEnd` (verifies `Hide()` + `IsSpeaking()==false` at end of dialogue), `TestManager_Update_SkipTyping` (verifies `EnableSpeechSkip` + Confirm completes spelling early). Verification: `go test ./internal/kit/ui/speech/ -cover` 89.8% (gate 80%). All 17 tests pass. No production code modified. File: `/Users/leandroatallah/www/go/ebiten/firefly/internal/kit/ui/speech/dialogue_test.go`.
- [Sonnet 4.6] [Workflow Gatekeeper] 2026-05-06 [APPROVED]: All quality gates passed. Coverage: `internal/kit/ui/speech/` 89.8% (gate 80%). Layer-rule: engine has zero `internal/kit` or `internal/game` deps; kit has zero `internal/game` deps (both confirmed by `go list -deps` exit 1 on grep). Full test suite green — zero failures across `./...`. golangci-lint: 0 issues. Primitives untouched (`engine/ui/speech/` contains only speech.go, speech_ext.go, font.go and their tests; no dialogue.go). `engine/ui/menu/` not touched. Constitution Kit row covers `internal/kit/` — no new row needed. No hardcoded game asset paths in production code (test file reference to "assets/audio/bleeps/bleep001.ogg" is confined to a test fixture, not production code). Coverage delta is positive: `internal/kit/ui/speech/` is a new package at 89.8% with no offsetting drop in engine or game suites.
