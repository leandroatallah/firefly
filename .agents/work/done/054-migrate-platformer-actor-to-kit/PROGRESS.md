# PROGRESS — 054-migrate-platformer-actor-to-kit

**Status:** Done

## Pipeline State

- [x] Story Architect
- [x] Spec Engineer
- [ ] Mock Generator
- [x] TDD Specialist
- [x] Feature Implementer
- [x] Workflow Gatekeeper

## Log

- [claude-sonnet-4-6] [Story Architect] 2026-05-06 [STARTED]: Creating USER_STORY.md for platformer actor relocation to kit layer.
- [claude-sonnet-4-6] [Story Architect] 2026-05-06 [FINISHED]: USER_STORY.md created. Pure relocation story — 5 ACs covering package compile at new path, old path removal, import reference updates across 10 known consumer files, existing test preservation, and full suite regression check. Flagged builder.go as the one potentially non-trivial edge case (engine-imports-kit violation).
- [claude-opus-4-7] [Spec Engineer] 2026-05-07: SPEC.md created. Story moved from backlog/ to active/. Key decisions: (1) Resolved the engine→kit violation in `internal/engine/entity/actors/builder/builder.go` by extracting only `PreparePlatformer` (the sole function that references the platformer concrete type) into a new `internal/kit/actors/platformer/builder.go` co-located with `PlatformerCharacter`. The engine builder retains all genre-agnostic helpers (`BuildStateMap`, `BodyRectFromSpriteData`, `SetCharacterBodies`, `SetCharacterStats`, `ConfigureCharacter`, `ApplyPlatformerPhysics`, `ApplySkills`, `collisionRectSetter`). Rejected (a) interface-abstraction and (c) reference-removal because the concrete `NewPlatformerCharacter` call must live in some package, and that package cannot be the engine layer. Rejected (b) wholesale move of `builder` to kit because most builder helpers are generic and would wrongly relocate. (2) Four game callers (`cody.go`, `climber.go`, `bat.go`, `wolf.go`) update both their import path and the call site (`builder.PreparePlatformer` → `platformer.PreparePlatformer`); the other six consumers update only the import. (3) No new interfaces; no behaviour changes; the Red Phase is encoded by two pre-existing layering tests (`internal/engine/layering_test.go` and `internal/engine/entity/actors/builder/builder_layering_test.go`) plus the 9 relocated `TestPlatformerCharacter_*` behavioural regression tests.
- [claude-opus-4-7] [TDD Specialist] 2026-05-07 [FINISHED]: Confirmed Red Phase encoded by pre-existing architectural tests; no new tests written (per spec, out of scope). Verified at HEAD: `TestEngineLayerHasNoKitOrGameDependencies/kit` (`internal/engine/layering_test.go:34`) PASS, `TestBuilderDoesNotImportKit` (`internal/engine/entity/actors/builder/builder_layering_test.go:13`) PASS, and all 9 `TestPlatformerCharacter_*` behavioural tests in `internal/engine/entity/actors/platformer/platformer_test.go` PASS. Hypothetical Red state: a naive move that only relocates files and rewrites imports (without extracting `PreparePlatformer`) would leave `internal/engine/entity/actors/builder/builder.go` importing `internal/kit/actors/platformer`, causing BOTH layering tests to fail — `TestBuilderDoesNotImportKit` flags the literal `"internal/kit/` substring in builder.go, and `TestEngineLayerHasNoKitOrGameDependencies/kit` flags the transitive `internal/kit/...` dependency in `go list -deps ./internal/engine/...`. The correct implementation (extracting `PreparePlatformer` to `internal/kit/actors/platformer/builder.go`) is what restores both layering tests to green; the 9 relocated platformer tests remain the safety net proving no behavioural drift.
- [claude-sonnet-4-6] [Feature Implementer] 2026-05-07 [FINISHED]: Relocated PlatformerCharacter from internal/engine/entity/actors/platformer/ to internal/kit/actors/platformer/ (platformer.go, platformer_test.go, builder.go); extracted PreparePlatformer into kit builder.go; removed PreparePlatformer and platformer import from engine builder; updated imports in all 10 consumer files with call-site rename in bat.go, wolf.go, cody.go, climber.go; deleted old engine platformer package. All tests pass including TestEngineLayerHasNoKitOrGameDependencies and TestBuilderDoesNotImportKit.
- [claude-sonnet-4-6] [Workflow Gatekeeper] 2026-05-07 [REJECTED]: golangci-lint (gofmt) fails on 3 files. The `kit/actors/platformer` import line was inserted in the wrong position within the single import block in each file — it must be sorted alphabetically with the other kit imports. The fix is `gofmt -w` on the three files or manual reordering. All other gates passed: AC-1 through AC-5 satisfied, old path deleted, no residual old import strings, all 9 TestPlatformerCharacter_* tests pass, TestEngineLayerHasNoKitOrGameDependencies/kit PASS, TestBuilderDoesNotImportKit PASS, full `go test ./...` clean, coverage delta positive (kit/actors/platformer gained 35.7% statement coverage). Backtrack to: Feature Implementer. Required fix: run `gofmt -w internal/game/entity/actors/enemies/bat.go internal/game/entity/actors/enemies/wolf.go internal/game/entity/actors/npcs/init_npcs.go` and re-verify golangci-lint passes.
- [claude-sonnet-4-6] [Workflow Gatekeeper] 2026-05-07 [APPROVED]: Re-validation after gofmt fix. All gates pass. `go test ./...` clean (all packages PASS). TestEngineLayerHasNoKitOrGameDependencies/kit PASS. TestBuilderDoesNotImportKit PASS. golangci-lint 0 issues. Coverage delta: internal/kit/actors/platformer gained 35.7% statement coverage (new package, previously 0%). Story complete.
