#!/usr/bin/env bash

if ! git diff --cached --quiet -- '.agents/skills/'; then
  make sync-skills
  git add .claude/skills/ .qwen/skills/ .kiro/skills/
fi

if ! git diff --cached --quiet -- '.agents/agents/'; then
  make sync-agents
  git add .claude/agents/ .qwen/agents/ .kiro/agents/
fi
